/* @flow */


const initialState = {
  lectureCount: 0,
  lectureProgress: 0,
  lectures: {},
};

const course = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};


export default course;

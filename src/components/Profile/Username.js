/**
 * @flow
 */
import React from 'react';
import ReactNative from 'react-native';

// const { Component, PropTypes } = React;
const { View } = ReactNative;

const Username = (): ReactElement<any> =>
  <View />;

Username.propTypes = {};
Username.defaultProps = {};

export default Username;

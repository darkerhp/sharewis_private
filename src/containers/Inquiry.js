/**
 * @flow
 */
import React from 'react';
import ReactNative from 'react-native';

// const { Component, PropTypes } = React;
const { View } = ReactNative;

const Inquiry = (): ReactElement<any> =>
  <View />;

Inquiry.propTypes = {};
Inquiry.defaultProps = {};

export default Inquiry;
